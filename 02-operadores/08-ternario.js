// uso del operador ternario (in mini if else)
//(Exprecion)?retorno del tru: retorno del false;

let edad = 24;
let acceso = edad > 17 ? 'Permitir ingreso' : 'No puede ingresar';
console.log(acceso);
// esto es un mini if 

